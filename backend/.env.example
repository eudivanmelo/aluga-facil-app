# Banco de dados
DB_PASSWORD=postgres

# JWT — TROQUE em produção (mínimo 32 caracteres)
JWT_KEY=ALTERE_ESTA_CHAVE_SECRETA_COM_MINIMO_32_CHARS!!

# pgAdmin (opcional, só sobe com --profile tools)
PGADMIN_EMAIL=admin@alugafacil.com
PGADMIN_PASSWORD=admin

# MinIO (armazenamento de imagens dos imóveis)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
